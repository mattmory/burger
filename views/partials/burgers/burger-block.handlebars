<li>
	{{name}}
		{{#unless eaten}}
        <button class="btn btn-secondary eat-burger" data-id="{{id}}">Eat it!</button>
        <input type="text" id="burger-{{id}}">
        {{/unless}}
        {{#if eaten}}
        - Eaten by {{eaten_by}}
        {{/if}}
</li>
